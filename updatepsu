#!/usr/bin/env bash
#

## Set download and install folders
dlfolder=~/Downloads
infolder=~/PSUnreal

## Get Latest Link
link=$(lynx -dump -listonly -nonumbers https://www.planeshift.it/Download | grep -oE 'https?://[^[:space:]"]+PSUnreal-Linux[^[:space:]"]+\.tar\.bz2' | head -n1)
printf "\nPull downloads page, parse and store link for latest PlaneShift Unreal for Linux package: $link\n"

## Get expected MD5 checksum from the same page (line: "PSUnreal Linux Client md5 checksum: <hash>")
expected_md5=$(lynx -dump https://www.planeshift.it/Download | sed -nE 's/.*PSUnreal Linux Client md5 checksum: ([a-f0-9]{32}).*/\1/p')
printf "\nParse expected MD5 checksum from downloads page: $expected_md5\n"

## Extract filename from Link
filename=$(echo "$link" | sed 's:.*/::')
printf "\nParsing and storing filename string: $filename\n"

## Remember filename with full path
fullpath=$dlfolder/$filename
printf "\nBuilding and Storing full path string: $fullpath\n"

## Parse latest version from filename (e.g. PSUnreal-Linux-0.7.55.tar.bz2 -> 0.7.55)
latest_version=$(echo "$filename" | sed -nE 's/^PSUnreal-Linux-([0-9.]+)\.tar\.bz2$/\1/p')
printf "\nLatest available version: $latest_version\n"

## Read installed version from the config.xml that ships in the package
## (line: <version name="X.Y.ZZ" />). Empty if file missing/malformed.
config_xml=$infolder/PSUnreal/Config/config.xml
if [ -f "$config_xml" ]; then
	installed_version=$(sed -nE 's/.*<version name="([^"]+)".*/\1/p' "$config_xml")
	printf "\nInstalled version: $installed_version\n"
else
	installed_version=""
	printf "\nNo existing install detected at $config_xml\n"
fi

## Skip if already on latest. If either side is empty (couldn't parse), fall through and install.
if [ -n "$latest_version" ] && [ "$latest_version" = "$installed_version" ]; then
	printf "\nAlready on latest version ($installed_version), nothing to do.\n"
	exit 0
fi

## Check if default Downloads folder does NOT exist
## It should really already exist on every linux Distro by default, but lets check anyway
## This will also fix user error if they specify a custom folder that doesn't exist
if [ ! -d "$dlfolder" ]; then
	## If not, create
	printf "\nDownload folder does not exist, creating...\n"
	mkdir -p $dlfolder
else
	printf "\nFolder $dlfolder exists, moving on...\n"
fi

## Check if file exists
if [ -f "$fullpath" ]; then
	printf "\n$filename already exists in $dlfolder, skipping download...\n"
else
	## If not, download
	printf "\n$filename does not exist in $dlfolder, downloading...\n"
	wget -P $dlfolder $link
fi

## Verify downloaded archive matches the published MD5 before touching install folder
## If we couldn't parse the expected hash, abort rather than extract an unverified archive
if [ -z "$expected_md5" ]; then
	printf "\nCould not parse expected MD5 from downloads page. Aborting before install.\n"
	exit 1
fi
actual_md5=$(md5sum "$fullpath" | awk '{print $1}')
if [ "$actual_md5" != "$expected_md5" ]; then
	printf "\nMD5 mismatch!\n  expected: $expected_md5\n  actual:   $actual_md5\nDownloaded file may be corrupted or stale. Delete $fullpath and re-run. Aborting.\n"
	exit 1
fi
printf "\nMD5 verified ($actual_md5), proceeding with install...\n"

## Check if install folder does NOT exist
if [ ! -d "$infolder" ]; then
	## If not, create
	printf "\nFolder $infolder doesn't exist, creating...\n"
	mkdir -p  $infolder
else
	printf "\nFolder $infolder exists, moving on...\n"
fi

## Check if folder empty
if [ -z "$(ls -A $infolder)" ]; then
	## If yes, safe to extract
	printf "\nFolder $infolder is empty, extracting $fullpath...\n"
	tar --recursive-unlink --strip-components=2 -xjvf $fullpath -C $infolder
else
	## If no, check with user first
	printf "\nFolder $infolder is not empty!!\nThis operation will delete contents of $infolder before extraction!!\nNote: PlaneShift Unreal for Linux Configs are safley stored in $HOME/.config/Epic/PSUnreal\n"
	read -p "Are you sure you want to continue (y/n)? " -n 1 -r
	if [[ $REPLY =~ ^[Yy]$ ]]; then
		## Echo new line for print clenlienss
		printf "\n"
		## User replied Y, clear $infolder and extract
		tar --recursive-unlink --strip-components=2 -xjvf $fullpath -C $infolder
	else
		printf "\nCancelling operation...\n"
	fi
fi
